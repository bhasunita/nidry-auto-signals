V3.7.1 INSTALL — SAME EASY METHOD

1. Rename current app.py to Old_v37_app.py
2. Create a new app.py
3. Paste the supplied app_v371.py content into the new app.py
4. Commit both changes to main
5. Let Render deploy, or use Manual Deploy -> Deploy latest commit
6. Open /health and confirm version 3.7.1
7. Test NIFTY, then switch to MCX -> CRUDEOIL MINI
8. If MCX data is unavailable, it must show DATA UNAVAILABLE and MUST NOT show NIFTY values

Important:
TradingView publicly lists MCX:CRUDEOILM1!, but MCX data may require registered access.
V3.7.1 therefore refuses to substitute NIFTY data when MCX data is unavailable.
Verify actual prices/contracts in your broker before any order.
